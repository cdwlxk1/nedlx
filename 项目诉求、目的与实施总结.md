# 链接签到系统项目诉求、目的与实施总结

## 一、项目背景

在日常工作和学习中，经常需要把多个微信文章、视频号内容或其他网页链接发送给一组人员查看。

如果只是在聊天工具中逐条发送链接，会出现以下问题：

- 链接分散，查找不方便；
- 无法清楚知道哪些人点击过链接；
- 无法确认哪些人已经完成查看；
- 用户需要反复填写姓名；
- 管理员无法集中查看签到结果；
- 每次新增或替换链接都需要重新整理消息。

因此，本项目希望把常用链接集中到一个网页中，并增加简单、清晰的查看和签到功能。

## 二、项目诉求

### 2.1 建立一个公共链接页面

项目需要提供一个网页，集中展示管理员发布的常用链接。

其他人员使用浏览器打开网页后，可以看到所有链接，并点击对应按钮跳转到原始内容页面。

链接内容可以是：

- 微信视频号内容；
- 微信公众号文章；
- 普通网页；
- 其他 HTTP 或 HTTPS 地址。

### 2.2 建立一个管理员页面

项目需要提供一个独立的管理员页面，用于管理公共页面中的链接。

管理员可以把多个链接一次性粘贴到输入框中，链接之间使用英文分号、中文分号或换行分隔。

程序自动识别链接，并生成对应的链接窗口，最多生成 6 个链接窗口。

### 2.3 实现用户签到

用户打开公共页面后，需要完成以下操作：

1. 填写自己的姓名；
2. 点击链接查看内容；
3. 返回页面；
4. 点击对应的“确定签到”按钮。

姓名只需要在公共浏览端填写一次，多个链接可以共用该姓名。

### 2.4 保存用户操作记录

系统需要记录：

- 谁点击了哪个链接；
- 点击时间是什么时候；
- 谁完成了哪个链接的签到；
- 签到时间是什么时候。

管理员需要能够查看这些记录。

### 2.5 发布到公网上

项目不能只在本地电脑中运行，还需要能够通过公网访问。

最初计划使用 GitHub Pages 发布网页，并通过后台服务和数据表保存签到信息。

## 三、项目目的

本项目的主要目的，是建立一个简单、稳定、易操作的链接查看和签到工具。

具体目标包括：

1. 将多个常用链接集中管理；
2. 减少管理员重复整理和发送链接的工作；
3. 让用户可以通过一个网页完成链接查看；
4. 让用户只填写一次姓名即可完成多个链接签到；
5. 让管理员能够集中查看点击和签到数据；
6. 通过公网地址方便地发送给其他人员；
7. 为以后绑定自定义域名、增加统计和身份认证功能保留扩展空间。

## 四、实施过程

## 4.1 第一阶段：确定网页功能

根据实际使用场景，首先将系统拆分为两个页面：

~~~text
index.html   公共浏览端
admin.html   管理员端
~~~

公共浏览端负责：

- 显示链接；
- 输入姓名；
- 打开链接；
- 完成签到。

管理员端负责：

- 输入多个链接；
- 自动解析链接；
- 发布链接；
- 使用密码保护管理操作。

## 4.2 第二阶段：尝试微信小程序方案

项目最初希望开发成微信小程序，并实现：

- 微信登录；
- 获取微信用户信息；
- 使用微信云开发；
- 将点击者信息写入云开发数据库；
- 管理员查看云端签到记录。

在实际配置过程中遇到了一些问题：

- 微信开发者工具无法选择微信云开发；
- 使用的 AppID 与当前登录账号或开发者身份不匹配；
- 测试号和正式 AppID 的权限不同；
- 云开发环境无法正常使用；
- 微信授权按钮没有反应；
- 开发者工具出现模拟器、调试器和网络连接问题。

这些问题导致微信小程序方案的部署成本较高，且无法快速形成稳定可用的版本。

## 4.3 第三阶段：改为普通网页方案

为了尽快实现核心功能，项目从微信小程序改为普通网页。

调整后的方案为：

~~~text
GitHub Pages
+ Cloudflare Worker
+ 飞书多维表格
~~~

其中：

- GitHub Pages 负责托管网页；
- Cloudflare Worker 负责后台接口；
- 飞书多维表格负责保存链接、点击和签到数据。

这种方案不再依赖微信云开发环境，也不需要处理微信开发者工具中的云开发配置问题。

## 4.4 第四阶段：设计飞书数据表

为了保存系统数据，在飞书多维表格中建立三张数据表：

~~~text
links
events
checkins
~~~

### links 表

用于保存管理员发布的链接：

~~~text
ID
标题
描述
链接
更新时间
~~~

### events 表

用于保存用户点击链接的记录：

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
点击时间
日期
~~~

### checkins 表

用于保存用户签到记录：

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
签到时间
日期
~~~

为了避免飞书接口出现字段类型转换错误，链接地址和时间字段采用文本类型。

## 4.5 第五阶段：开发 Cloudflare Worker

Cloudflare Worker 作为前端和飞书之间的中间层，负责：

- 验证管理员密码；
- 接收管理员发布的链接；
- 将链接保存到 links 表；
- 读取公共页面需要显示的链接；
- 保存用户点击事件；
- 保存或更新用户签到记录；
- 读取管理员数据；
- 调用飞书开放接口；
- 处理网页跨域请求。

飞书 App Secret 和管理员密码只放在 Worker 的服务端环境变量中，不放入 GitHub 前端代码。

## 4.6 第六阶段：配置网页与 Worker

网页通过 cloud-config.js 获取 Worker 地址。

当前配置逻辑如下：

~~~js
window.LINK_HUB_CONFIG = Object.freeze({
  apiUrl: "https://link-checkin-api.cdwlxk.workers.dev"
});
~~~

Worker 通过 ALLOWED_ORIGIN 限制允许访问的网页来源。

当前 GitHub Pages 来源为：

~~~text
https://cdwlxk1.github.io
~~~

## 4.7 第七阶段：发布到 GitHub Pages

项目上传到 GitHub 仓库：

~~~text
https://github.com/cdwlxk1/nedlx
~~~

GitHub Pages 设置为：

~~~text
Source：Deploy from a branch
Branch：main
Folder：/ root
~~~

部署完成后，网页可以通过以下地址访问：

公共页面：

~~~text
https://cdwlxk1.github.io/nedlx/
~~~

管理员页面：

~~~text
https://cdwlxk1.github.io/nedlx/admin.html
~~~

## 4.8 第八阶段：处理部署和接口问题

实施过程中重点解决了以下问题：

### GitHub Pages 无法访问

通过检查 GitHub Pages 发布来源、分支和目录，最终确认使用 main 分支根目录发布。

### Worker Preview 配置错误

使用 Cloudflare Worker 正式 workers.dev 地址，避免使用 Preview 地址。

### Failed to fetch

检查并修正：

- Worker 地址；
- ALLOWED_ORIGIN；
- GitHub 上的 cloud-config.js；
- 浏览器缓存；
- Worker 是否部署成功。

### URLFieldConvFail

将飞书链接地址字段改为文本类型。

### DatetimeFieldConvFail

将飞书时间字段改为文本类型，并按文本方式保存时间。

### 管理员密码重复使用

管理员密码保存在 Worker 环境变量中，每次发布时进行验证，不使用一次性密码机制。

## 五、最终实现效果

### 5.1 管理员使用效果

管理员打开：

~~~text
https://cdwlxk1.github.io/nedlx/admin.html
~~~

输入多个链接，例如：

~~~text
https://weixin.qq.com/sph/AQk4HX6E6v;
https://mp.weixin.qq.com/s/YeI2lXYCQ9_Zal0_oq1USA
~~~

输入管理员密码后，点击“发布到公共端”，系统会自动解析链接并发布。

管理员密码可以重复使用，不会因为发布一次而失效。

### 5.2 普通用户使用效果

用户打开：

~~~text
https://cdwlxk1.github.io/nedlx/
~~~

然后：

1. 填写一次姓名；
2. 点击某个链接的“打开链接”；
3. 阅读对应内容；
4. 返回签到页面；
5. 点击“确定签到”。

每个链接都有独立的查看和签到状态。

### 5.3 后台记录效果

用户操作后，记录会保存到飞书多维表格：

- links 表保存管理员发布的链接；
- events 表保存用户点击记录；
- checkins 表保存用户签到记录。

管理员可以直接打开飞书多维表格查看：

- 用户姓名；
- 点击了哪些链接；
- 哪些链接已经签到；
- 点击和签到的具体时间；
- 每日签到情况。

### 5.4 当前部署结果

当前系统已经形成以下可用结构：

~~~text
公共网页：GitHub Pages
管理后台：GitHub Pages 上的 admin.html
后台接口：Cloudflare Worker
数据存储：飞书多维表格
~~~

## 六、当前方案的边界

当前系统已经实现了“链接发布、链接查看、姓名填写、点击记录和签到记录”功能，但仍有以下边界：

1. 当前是普通网页，不是微信小程序；
2. 当前不提供微信实名认证登录；
3. 姓名由用户自行填写，不能完全证明真实身份；
4. visitorId 只能识别浏览器设备，不能等同于微信 openid；
5. 公共页面属于公开网页，任何获得网址的人都可以访问；
6. 管理员页面使用密码保护，但不是完整的企业级权限系统；
7. 飞书接口或 Cloudflare Worker 出现故障时，签到可能暂时无法提交。

## 七、后续可发展方向

后续可以继续增加：

- 自定义域名；
- 管理员网页直接查看签到表；
- 按日期和姓名筛选；
- 导出 Excel 或 CSV；
- 链接有效期；
- 删除或停用链接；
- 多管理员账号；
- 微信登录；
- 飞书登录；
- 签到统计图表；
- 管理员操作日志；
- 更严格的防重复签到机制。

## 八、项目结论

本项目最终解决的是一个具体的实际问题：

~~~text
把分散的常用链接集中起来，
让用户方便查看，
让用户填写一次姓名完成签到，
让管理员能够在后台看到真实的点击和签到记录。
~~~

经过从微信小程序方案到普通网页方案的调整，项目目前采用 GitHub Pages、Cloudflare Worker 和飞书多维表格组成的轻量化架构，已经具备公开访问、管理员发布、用户签到和后台记录查看等核心能力。

