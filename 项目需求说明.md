# 链接签到系统项目需求说明

## 1. 文档目的

本文档用于记录项目发起人的原始诉求，以及开发过程中对这些诉求形成的具体产品和技术要求。

本文档可以作为：

- 后续继续开发的依据；
- 修改功能时的检查清单；
- 部署和交接时的说明；
- 项目验收时的参考标准。

## 2. 用户提出的原始诉求

### 2.1 常用链接集中展示

用户希望把一些常用链接集中放到一个网页中，方便发送给其他人。

其他人使用浏览器打开网页后，可以点击相应链接，直接跳转到对应的微信文章、视频号或其他网页。

初始示例链接包括：

~~~text
https://weixin.qq.com/sph/AQk4HX6E6v
https://mp.weixin.qq.com/s/YeI2lXYCQ9_Zal0_oq1USA
~~~

### 2.2 用户查看后签到

其他人打开链接查看内容后，需要在对应链接后填写自己的姓名，再点击“确定签到”，代表已经完成查看和签到。

用户希望自己的姓名能够被记录下来，管理员可以查看所有人的签到情况。

### 2.3 管理员批量输入链接

管理员希望有一个单独的管理页面，可以把多个链接一次性粘贴进去。

链接之间可以使用：

- 英文分号；
- 中文分号；
- 换行。

程序自动识别这些链接，并生成多个可点击的链接窗口。

页面最多生成 6 个链接窗口。

### 2.4 公共浏览端和管理员端分开

用户明确要求分成两个网页：

~~~text
index.html   公共浏览端
admin.html   管理员端
~~~

公共浏览端只供其他人浏览链接和签到。

管理员端供管理员输入链接、发布链接和查看管理数据。

### 2.5 姓名填写方式

用户填写自己的姓名后，同一页面中的多个链接可以共用这个姓名，不需要每个链接重复填写。

姓名填写区域只在公共浏览端出现一次。

### 2.6 记录点击者信息

用户点击链接后，系统需要记录点击者的信息，包括：

- 用户填写的姓名；
- 访问设备标识；
- 点击的链接；
- 点击时间。

用户点击“确定签到”后，系统还需要记录：

- 签到姓名；
- 对应链接；
- 签到时间；
- 签到日期。

这些信息需要保存到后台，管理员能够查看。

### 2.7 管理员密码

管理员端需要密码保护。

管理员密码不能使用一次就失效，管理员可以重复使用同一个密码发布链接和查看数据，直到主动修改密码。

### 2.8 公网发布

用户希望将程序发布到公网上，方便通过浏览器访问，而不是只能在本地电脑运行。

项目计划发布到 GitHub Pages，并可以进一步绑定自己的域名。

## 3. 开发过程中形成的方案调整

项目最初考虑使用微信小程序、微信登录和微信云开发。

实际操作中遇到以下问题：

- 微信开发者工具中无法选择微信云开发；
- AppID 与当前登录账号或开发者身份不匹配；
- 测试号和正式小程序 AppID 存在权限限制；
- 云开发环境无法正常使用；
- 部分微信授权按钮没有反应；
- 开发者工具出现模拟器、调试器和云服务连接问题。

因此，项目方案调整为：

~~~text
普通网页
+ GitHub Pages
+ Cloudflare Worker
+ 飞书多维表格
~~~

当前版本不再依赖：

- 微信小程序；
- 微信云开发环境 ID；
- 微信云函数；
- 微信授权登录。

## 4. 我理解后的产品要求

### 4.1 用户角色

系统包含两类用户。

### 管理员

管理员可以：

- 输入多个链接；
- 发布链接到公共浏览端；
- 使用密码验证管理员身份；
- 查看用户点击记录；
- 查看用户签到记录。

### 普通用户

普通用户可以：

- 打开公共浏览端；
- 填写一次自己的姓名；
- 查看链接列表；
- 点击链接跳转到原网页；
- 点击“确定签到”完成签到。

普通用户不能：

- 发布或修改链接；
- 查看其他人的后台记录；
- 修改系统配置。

### 4.2 公共浏览端要求

公共浏览端文件为：

~~~text
index.html
~~~

页面需要显示：

- 系统标题；
- 链接总数；
- 当前已查看或已签到的状态；
- 用户姓名输入框；
- 每个链接的标题、描述和地址；
- “打开链接”按钮；
- “确定签到”按钮；
- 签到成功或失败提示。

用户姓名输入框只显示一次，并作为当前页面所有链接的默认签到姓名。

用户点击“打开链接”后，系统记录一次点击事件。

用户点击“确定签到”后，系统向后台提交签到记录。

同一用户当天对同一链接重复签到时，系统应更新已有签到记录，避免无限生成重复数据。

### 4.3 管理员端要求

管理员端文件为：

~~~text
admin.html
~~~

页面需要提供：

- 多链接输入框；
- 管理员密码输入框；
- “发布到公共端”按钮；
- 发布结果提示；
- 后台记录查看区域。

链接输入规则：

- 支持英文分号分隔；
- 支持中文分号分隔；
- 支持换行分隔；
- 自动清理空白内容；
- 忽略空行；
- 只接受 HTTP 或 HTTPS 链接；
- 最多生成 6 个链接窗口。

管理员密码应由服务端验证，不能只在浏览器前端验证。

### 4.4 链接管理要求

每条链接至少包含：

- 唯一 ID；
- 标题；
- 描述；
- 链接地址；
- 更新时间；
- 排序位置。

管理员重新发布链接时，公共页面应显示最新发布的链接列表。

### 4.5 点击记录要求

每次用户点击“打开链接”，系统记录一条点击事件。

点击记录至少包含：

- visitorId；
- 姓名；
- 链接 ID；
- 链接标题；
- 链接地址；
- 点击时间；
- 日期。

### 4.6 签到记录要求

每次用户点击“确定签到”，系统记录或更新一条签到记录。

签到记录至少包含：

- visitorId；
- 姓名；
- 链接 ID；
- 链接标题；
- 链接地址；
- 签到时间；
- 日期。

签到的唯一判断条件为：

~~~text
同一个 visitorId + 同一天 + 同一个链接
~~~

## 5. 数据存储要求

当前使用飞书多维表格保存数据。

需要创建三张数据表：

~~~text
links
events
checkins
~~~

### 5.1 links 表

~~~text
ID
标题
描述
链接
更新时间
~~~

### 5.2 events 表

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
点击时间
日期
~~~

### 5.3 checkins 表

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
签到时间
日期
~~~

为降低接口字段类型转换错误，链接地址和时间字段建议使用文本类型。

## 6. 后端接口要求

后端使用 Cloudflare Worker 提供接口。

Worker 负责：

- 管理员密码验证；
- 读取链接列表；
- 发布和替换链接；
- 保存点击事件；
- 保存或更新签到记录；
- 读取管理员数据；
- 调用飞书开放接口；
- 处理跨域请求；
- 隐藏飞书 App Secret。

前端只能保存 Worker 的公网地址，不能保存飞书 App Secret。

## 7. 安全要求

https://dash.cloudflare.com/ab7218214f6594b0dc751370617706bf/workers/services/view/link-checkin-api/production/settings

### 7.1 密钥保护

以下内容只能配置在 Cloudflare Worker 的服务端环境变量中：

- FEISHU_APP_SECRET；
- ADMIN_PASSWORD。

不得将这些内容上传到 GitHub 或写入 HTML、JavaScript 前端文件。

### 7.2 跨域限制

Worker 应通过 ALLOWED_ORIGIN 限制允许访问的网页来源。

当前 GitHub Pages 地址为：

~~~text
https://cdwlxk1.github.io
~~~

绑定自定义域名后，应将 ALLOWED_ORIGIN 修改为新的网页来源。

### 7.3 管理员权限

管理员接口必须在服务端验证管理员密码。

普通用户接口不应返回管理员数据。

### 7.4 身份边界

当前姓名由用户自行填写，visitorId 由浏览器生成。

因此当前系统能够记录“用户填写的姓名”和“访问设备”，但不能证明姓名一定对应真实身份。

如果以后需要真实微信身份认证，需要另外接入微信登录或其他身份认证服务。

## 8. 部署要求

### 8.1 GitHub Pages

代码发布到 GitHub 仓库：

~~~text
https://github.com/cdwlxk1/nedlx
~~~

GitHub Pages 应设置为：

~~~text
Source：Deploy from a branch
Branch：main
Folder：/ root
~~~

公共页面：

~~~text
https://cdwlxk1.github.io/nedlx/
~~~

管理员页面：

~~~text
https://cdwlxk1.github.io/nedlx/admin.html
~~~

### 8.2 Cloudflare Worker

Worker 正式地址：

~~~text
https://link-checkin-api.cdwlxk.workers.dev
~~~

环境变量应配置在 Production 环境，而不是 Preview 环境。

### 8.3 前端 Worker 配置

cloud-config.js 只配置 Worker 公网地址：

~~~js
window.LINK_HUB_CONFIG = Object.freeze({
  apiUrl: "https://link-checkin-api.cdwlxk.workers.dev"
});
~~~

## 9. 自定义域名要求

用户可以在 Cloudflare、阿里云或腾讯云购买域名。

绑定域名后，建议使用：

~~~text
https://www.你的域名.com/
https://www.你的域名.com/admin.html
~~~

DNS 中添加：

~~~text
Type：CNAME
Name：www
Target：cdwlxk1.github.io
~~~

Target 不应包含项目目录 /nedlx。

完成域名绑定后，需要同步修改：

~~~text
ALLOWED_ORIGIN=https://www.你的域名.com
~~~

## 10. 功能验收标准

### 10.1 管理员发布验收

- [ ] 管理员页面可以打开；
- [ ] 输入多个分号分隔的链接可以正常识别；
- [ ] 输入多行链接可以正常识别；
- [ ] 最多生成 6 个链接窗口；
- [ ] 错误链接会被拒绝；
- [ ] 正确密码可以发布；
- [ ] 错误密码不能发布；
- [ ] 密码可以重复使用；
- [ ] 发布后公共页面显示最新链接。

### 10.2 普通用户验收

- [ ] 公共页面可以打开；
- [ ] 姓名只需填写一次；
- [ ] 用户可以打开每个链接；
- [ ] 点击链接后能记录点击事件；
- [ ] 用户可以完成签到；
- [ ] 签到后显示成功提示；
- [ ] 同一用户当天重复签到不会无限新增重复记录。

### 10.3 后台数据验收

- [ ] links 表出现管理员发布的链接；
- [ ] events 表出现用户点击记录；
- [ ] checkins 表出现用户签到记录；
- [ ] 姓名、链接地址和时间显示正确；
- [ ] 管理员能够在飞书多维表格中查看记录。

### 10.4 部署验收

- [ ] GitHub Pages 公共地址可以访问；
- [ ] 管理员地址可以访问；
- [ ] Worker 正式地址可用；
- [ ] Production 环境变量完整；
- [ ] GitHub Actions 部署成功；
- [ ] 浏览器不会出现 Failed to fetch；
- [ ] HTTPS 可以正常使用。

## 11. 已知限制

当前系统存在以下边界：

1. 当前不是微信小程序，而是普通网页；
2. 当前不提供微信实名认证登录；
3. 用户姓名由用户自行填写；
4. visitorId 只能识别浏览器设备，不能等同于真实用户；
5. 公共页面属于公开网页，任何获得网址的人都可以访问；
6. GitHub Pages 负责静态页面，Cloudflare Worker 负责后台接口；
7. 飞书多维表格权限或接口异常时，签到可能暂时无法提交；
8. 域名解析和 HTTPS 证书生效可能需要等待一段时间。

## 12. 后续可扩展功能

如果以后继续开发，可以增加：

- 管理员直接在网页中查看签到数据；
- 按日期筛选签到记录；
- 导出 Excel 或 CSV；
- 删除或停用某个链接；
- 设置链接有效期；
- 设置每天只能签到一次；
- 多管理员账号；
- 微信登录；
- 飞书登录；
- 签到统计图表；
- 用户重复签到提醒；
- 管理员操作日志；
- 自定义页面标题和封面。

## 13. 当前项目结论

当前项目的核心目标是：

~~~text
管理员发布链接
        ↓
用户打开并查看链接
        ↓
用户填写姓名并签到
        ↓
点击与签到数据保存到飞书
~~~

当前推荐技术方案是：

~~~text
GitHub Pages：托管公共页面和管理员页面
Cloudflare Worker：提供安全后台接口
飞书多维表格：保存链接、点击和签到数据
自定义域名：提供更方便的访问地址
~~~
