# 链接签到系统部署与使用指南

## 一、系统简介

这是一个“常用链接发布与签到系统”，适合将多个微信文章、视频号或其他网页链接集中展示。

系统支持：

- 管理员批量输入多个链接；
- 使用英文分号、中文分号或换行分隔链接；
- 自动生成最多 6 个可点击的链接窗口；
- 用户打开链接后填写自己的姓名；
- 用户点击“确定签到”完成签到；
- 点击记录和签到记录保存到飞书多维表格；
- 管理员可以在飞书后台查看所有记录。

系统结构：

~~~text
管理员页面 admin.html
        ↓
Cloudflare Worker
        ↓
飞书多维表格
        ↑
公共页面 index.html
~~~

## 二、项目文件

项目目录：

~~~text
D:\CODEV202303\doc\link-checkin
~~~

主要文件：

~~~text
index.html          公共浏览端
admin.html          管理员端
app.js              公共端逻辑
admin.js            管理端逻辑
styles.css          页面样式
cloud-config.js     Worker 地址配置
feishu-worker.js    Cloudflare Worker 后端
FEISHU_SETUP.md     飞书配置说明
README.md           项目说明
~~~

## 三、网页地址

GitHub 仓库：

[https://github.com/cdwlxk1/nedlx](https://github.com/cdwlxk1/nedlx)

当前公共页面：

~~~text
https://cdwlxk1.github.io/nedlx/
~~~

当前管理页面：

~~~text
https://cdwlxk1.github.io/nedlx/admin.html
~~~

## 四、创建飞书多维表格

### 4.1 创建三张数据表

创建一个飞书多维表格，并建立以下三张数据表：

~~~text
links
events
checkins
~~~

### 4.2 links 表字段

~~~text
ID
标题
描述
链接
更新时间
~~~

### 4.3 events 表字段

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
点击时间
日期
~~~

### 4.4 checkins 表字段

~~~text
visitorId
姓名
链接ID
链接标题
链接地址
签到时间
日期
~~~

建议以上字段全部使用“文本”类型，尤其是链接地址、点击时间、签到时间和更新时间字段。这样可以避免：

~~~text
URLFieldConvFail
DatetimeFieldConvFail
~~~

飞书表格的字段名必须与代码中的字段名完全一致。

## 五、创建飞书自建应用

在飞书开放平台创建自建应用，并获取：

~~~text
App ID
App Secret
~~~

同时获取多维表格信息：

~~~text
App Token
links Table ID
events Table ID
checkins Table ID
~~~

多维表格链接通常类似：

~~~text
https://my.feishu.cn/base/P32xxxxxxxxxxxx?table=tblxxxxxxxxxxxx
~~~

其中：

- /base/P32... 部分是 App Token；
- table=tbl... 后面的内容是对应数据表的 Table ID。

将飞书自建应用添加到多维表格，并授予读取、创建、修改和删除记录的权限。

注意：

- App Secret 不能上传到 GitHub；
- 管理员密码不能写入网页前端代码；
- App Secret 和管理员密码只能配置在 Cloudflare Worker 的服务端变量中。

## 六、部署 Cloudflare Worker

将项目中的 feishu-worker.js 部署为 Cloudflare Worker。

部署成功后，会得到类似下面的正式地址：

~~~text
https://你的Worker名称.你的账号.workers.dev
~~~

当前项目使用的 Worker 地址：

~~~text
https://link-checkin-api.cdwlxk.workers.dev
~~~

### 6.1 配置环境变量

在 Cloudflare Worker 中进入：

~~~text
Settings → Variables and Secrets
~~~

确认变量配置在 Production 环境，而不是 Preview 环境。

配置以下变量：

~~~text
FEISHU_APP_ID=你的飞书App ID
FEISHU_APP_SECRET=你的飞书App Secret
FEISHU_APP_TOKEN=多维表格App Token
FEISHU_LINKS_TABLE_ID=links表的Table ID
FEISHU_EVENTS_TABLE_ID=events表的Table ID
FEISHU_CHECKINS_TABLE_ID=checkins表的Table ID
ADMIN_PASSWORD=管理员密码
ALLOWED_ORIGIN=https://cdwlxk1.github.io
~~~

建议：

- FEISHU_APP_SECRET 类型选择 Secret；
- ADMIN_PASSWORD 类型选择 Secret；
- 其他普通配置可以选择 Plaintext；
- 保存变量后重新部署 Worker。

如果看到 Invalid Workers Preview configuration，说明使用了 Preview 地址或 Preview 配置，应改用正式的 workers.dev 地址并重新部署生产版本。

## 七、配置网页端 Worker 地址

打开项目中的 cloud-config.js，填写 Worker 正式地址：

~~~js
window.LINK_HUB_CONFIG = Object.freeze({
  apiUrl: "https://link-checkin-api.cdwlxk.workers.dev"
});
~~~

不要把 FEISHU_APP_SECRET、ADMIN_PASSWORD 等内容写入这个文件。

## 八、发布到 GitHub Pages

将以下文件上传到 GitHub 仓库根目录：

~~~text
index.html
admin.html
app.js
admin.js
styles.css
cloud-config.js
~~~

进入 GitHub 仓库：

~~~text
Settings → Pages
~~~

设置：

~~~text
Source：Deploy from a branch
Branch：main
Folder：/ root
~~~

保存后等待 GitHub Actions 完成部署。

如果 Actions 中 build 成功但 deploy 失败，优先检查 Pages 的发布来源是否为 main / root。

每次修改代码后：

1. 上传修改后的文件；
2. 等待 GitHub Actions 完成；
3. 使用浏览器无痕窗口访问；
4. 如果仍显示旧页面，清理浏览器缓存。

## 九、管理员发布链接

打开管理员页面：

~~~text
https://cdwlxk1.github.io/nedlx/admin.html
~~~

操作步骤：

1. 在输入框粘贴多个链接；
2. 使用英文分号、中文分号或换行分隔；
3. 输入管理员密码；
4. 点击“发布到公共端”。

示例：

~~~text
https://weixin.qq.com/sph/AQk4HX6E6v;
https://mp.weixin.qq.com/s/YeI2lXYCQ9_Zal0_oq1USA
~~~

管理员密码不是一次性密码。只要 Cloudflare Worker 中的 ADMIN_PASSWORD 不变，就可以重复发布链接。

## 十、用户签到流程

用户打开公共页面：

~~~text
https://cdwlxk1.github.io/nedlx/
~~~

操作步骤：

1. 填写自己的姓名；
2. 点击对应的“打开链接”；
3. 阅读链接内容；
4. 返回签到页面；
5. 点击对应的“确定签到”。

系统会将访问设备标识、用户姓名、链接 ID、链接标题、链接地址、点击时间、签到时间和日期保存到飞书。

点击记录保存在 events 表，签到记录保存在 checkins 表。

## 十一、查看后台数据

直接打开飞书多维表格，查看：

~~~text
events
checkins
~~~

- events 用于查看用户点击了哪些链接；
- checkins 用于查看用户是否完成签到；
- 每个用户每天对同一个链接重复签到时，系统会更新原记录，而不是无限新增重复记录。

## 十二、常见错误处理

### 12.1 Failed to fetch

通常原因：

- Worker 地址填写错误；
- Worker 没有部署成功；
- ALLOWED_ORIGIN 不正确；
- GitHub 仍在使用旧版 cloud-config.js；
- 浏览器缓存了旧文件。

检查 cloud-config.js、Worker 正式地址和 ALLOWED_ORIGIN。

### 12.2 URLFieldConvFail

说明飞书字段“链接地址”使用了 URL 类型，而代码按文本写入。解决方法是把链接地址字段改为文本类型。

### 12.3 DatetimeFieldConvFail

说明飞书时间字段类型与代码不匹配。解决方法是把点击时间、签到时间和更新时间改为文本类型。

### 12.4 微信授权无反应

这是之前微信小程序方案中的问题，常见原因包括使用测试 AppID、当前账号不是小程序开发者、云开发环境无法选择、微信开发者工具网络异常或云开发权限不足。

当前系统已经改为：

~~~text
普通网页 + 飞书多维表格 + Cloudflare Worker
~~~

因此当前版本不再依赖微信云开发环境 ID、微信云函数和微信授权。

网页中的姓名来自用户手动填写，不等同于微信实名认证身份。

## 十三、绑定自己的域名

域名可以在 Cloudflare、阿里云或腾讯云购买。

购买域名后，先在 GitHub 仓库中进入：

~~~text
Settings → Pages → Custom domain
~~~

填写：

~~~text
www.你的域名.com
~~~

然后在 Cloudflare DNS 中添加：

~~~text
Type：CNAME
Name：www
Target：cdwlxk1.github.io
Proxy status：DNS only
~~~

Target 只填写 cdwlxk1.github.io，不要填写 cdwlxk1.github.io/nedlx。

回到 GitHub Pages，等待 HTTPS 证书生成，再开启 Enforce HTTPS。

最后把 Cloudflare Worker 中的 ALLOWED_ORIGIN 修改为新网页地址，例如：

~~~text
https://www.你的域名.com
~~~

然后重新部署 Worker。

新地址将变为：

~~~text
https://www.你的域名.com/
https://www.你的域名.com/admin.html
~~~

## 十四、最终上线检查清单

### 网页端

- [ ] 公共页面可以打开；
- [ ] 管理员页面可以打开；
- [ ] cloud-config.js 使用 Worker 正式地址；
- [ ] 管理员密码可以重复使用；
- [ ] 用户可以填写姓名；
- [ ] 用户点击链接后可以签到。

### Cloudflare Worker

- [ ] Worker 使用正式 workers.dev 地址；
- [ ] 变量配置在 Production；
- [ ] FEISHU_APP_SECRET 已配置；
- [ ] ADMIN_PASSWORD 已配置；
- [ ] ALLOWED_ORIGIN 与网页地址一致；
- [ ] 没有把密钥上传到 GitHub。

### 飞书多维表格

- [ ] 已创建 links、events、checkins 三张表；
- [ ] 字段名称完全一致；
- [ ] 链接和时间字段建议使用文本类型；
- [ ] 飞书应用已经添加到多维表格；
- [ ] 应用拥有记录读写权限；
- [ ] App Token 和三个 Table ID 正确。

### GitHub Pages

- [ ] Pages 来源为 main / root；
- [ ] GitHub Actions 构建成功；
- [ ] 页面文件已上传到仓库根目录；
- [ ] 自定义域名解析正确；
- [ ] HTTPS 已开启。

## 十五、安全提醒

当前系统是公开网页，任何拿到网址的人都可以访问公共页面。建议：

- 使用较复杂的管理员密码；
- 不要把管理员密码写进网页代码；
- 不要把飞书 App Secret 上传到 GitHub；
- 定期检查飞书多维表格的应用权限；
- 如果需要真正的微信身份认证，还需要单独接入微信登录或其他身份认证服务。

